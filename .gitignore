__pycache__/
*.py[cod]
.venv/
node_modules/
frontend/dist/
src-tauri/target/
outputs/
*.db
*.bin
.vsm-ocr/
~/.cache/vsm-ocr/
*.gguf
*.pt
*.onnx
# Modèles NER téléchargés localement (DrBERT-CASM2, exports ONNX) — jamais
# commités : vendorisés dans l'installeur par packaging/fetch_models.py.
models/
.pytest_cache/
.ruff_cache/
frontend/tsconfig.tsbuildinfo

# Documents de communication (règlement du concours, données fournies) :
# CONSERVÉS LOCALEMENT UNIQUEMENT — jamais commités ni poussés sur GitHub.
reglements/
# Captures d'écran de diagnostic (bug) — locales uniquement.
bug/
# Lanceur local (contient la phrase du coffre-fort de CE poste) — jamais committé.
lancer_vsm.bat
