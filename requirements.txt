# VSM-OCR — dépendances Python (toutes locales, aucune ne fait d'appel cloud)
# Python 3.12
# Roues CPU précompilées de llama-cpp-python (Windows/Linux, toutes versions
# Python) — PyPI n'héberge que le code source de ce paquet.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
python-multipart>=0.0.9        # upload de fichiers FastAPI
cryptography>=42.0             # AES-256-GCM
argon2-cffi>=23.1              # dérivation de clé / hash mots de passe
rapidfuzz>=3.6                 # normalisation fuzzy CIM-10 / ATC
jsonschema>=4.21               # validation du contrat vsm_schema.json
pytesseract>=0.3.10            # OCR (binaire tesseract + pack fra requis)
pdf2image>=1.17                # PDF -> images (binaire poppler requis)
pillow>=10.2
numpy>=1.26
reportlab>=4.1                 # rendu PDF du VSM
jiwer>=3.0                     # CER/WER pour le benchmark
psutil>=5.9                    # cœurs PHYSIQUES (llama.cpp n_threads)
llama-cpp-python>=0.2          # moteur NLP LLM local OBLIGATOIRE (phase LLM
                               # par document : correction OCR + extraction ;
                               # roue CPU précompilée via l'index ci-dessus —
                               # aucun appel cloud, jamais de compilation)

# --- optionnels (adaptateurs, non requis pour le fonctionnement nominal) ---
# torch + transformers       # (1) NER DrBERT-MedicalNER-FR (licence « style
#                            #     OpenRAIL », usage commercial permis — docs/ADR/0010) ;
#                            # (2) moteur OCR « unlimited » (baidu, MIT — docs/ADR/0005).
#                            # transformers épinglé >=4.53,<5 (la 5.x casse le
#                            # tokenizer CamemBERT). Présents seulement dans
#                            # l'environnement Python 3.12 (extraction).
# spacy>=3.7                   # détection PII renforcée (fr_core_news_lg)
# python-doctr / paddleocr     # moteurs OCR alternatifs pour le benchmark
